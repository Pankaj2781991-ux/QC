# Firebase Functions (Node) environment
#
# This file is an EXAMPLE ONLY. Do not put real secrets here.
# Copy to `apps/functions/.env` for local dev (if you use dotenv),
# or set these via `firebase functions:config:set` / runtime env as appropriate.

# Project identifiers (NOT secrets)
GCLOUD_PROJECT="YOUR_PROJECT_ID"
FIREBASE_PROJECT_ID="YOUR_PROJECT_ID"
GOOGLE_CLOUD_PROJECT="YOUR_PROJECT_ID"

# Optional: region your Functions are deployed to (NOT secret)
FUNCTIONS_REGION="us-central1"

# Optional: emulator overrides for local development (NOT secrets)
# FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"
# FIREBASE_AUTH_EMULATOR_HOST="127.0.0.1:9099"
# FIREBASE_STORAGE_EMULATOR_HOST="127.0.0.1:9199"

# Secrets (DO NOT put actual values here)
# Store real secrets in Google Secret Manager and reference them by name.
# Example placeholders:
# INTEGRATION_API_KEY_SECRET_RESOURCE="projects/YOUR_PROJECT_ID/secrets/tenant-<TENANT_ID>-integration-<INTEGRATION_ID>-apiKey/versions/latest"
