# Next.js (Vercel) + Firebase Web SDK
#
# This file is an EXAMPLE ONLY. Do not put real secrets here.
# Copy to `apps/web/.env.local` for local dev.
# In Vercel, add these as Project Environment Variables.

# Firebase Web SDK config (PUBLIC — safe to expose to the browser)
NEXT_PUBLIC_FIREBASE_API_KEY="YOUR_PUBLIC_API_KEY"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="YOUR_PROJECT_ID.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="YOUR_PROJECT_ID"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="YOUR_PROJECT_ID.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="YOUR_SENDER_ID"
NEXT_PUBLIC_FIREBASE_APP_ID="1:YOUR_SENDER_ID:web:YOUR_APP_ID"

# Optional (only if you enable Analytics)
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-XXXXXXXXXX"

# Backend base URL for the Functions API (PUBLIC)
# For local emulators this is typically: http://127.0.0.1:5001/YOUR_PROJECT_ID/us-central1/api
# For deployed Gen2 HTTPS functions you can also use the Cloud Run URL shown after deploy.
# Example: https://api-<hash>-uc.a.run.app
NEXT_PUBLIC_FUNCTIONS_BASE_URL="https://api-<hash>-uc.a.run.app"
